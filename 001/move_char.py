#!/usr/bin/env python

def revert_string(sentence, move_times):
   
   for (i )
     

   return sentence


def main():
  # The input the this question
  sentence = "abcdef"
  move_times = 3

  # Call the method to revert string
  result = revert_string(sentence, move_times)

  # Print the result, should be "defabc"
  print(result)

if __name__ == "__main__":
  main()
